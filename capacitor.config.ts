import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.capicator.nest.desktop',
  appName: 'NEST Desktop',
  webDir: 'nest_desktop/app',
  bundledWebRuntime: false
};

export default config;
